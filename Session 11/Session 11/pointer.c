#include <stdio.h>

/*
Pointer adalah variabel yang menyimpan alamat memori dari variabel lain. 
Ini memungkinkan pengelolaan memori secara lebih efisien dan memungkinkan manipulasi langsung data dalam memori.
*/

int main() {
    // int var = 10;     // deklarasi variabel integer
    // int *ptr;        // deklarasi pointer untuk integer

    // ptr = &var;      // pointer ptr menyimpan alamat memori dari variabel var

    // // menampilkan nilai dan alamat memori dari var
    // printf("Nilai dari var: %d\n", var); // 10
    // printf("Alamat memori dari var: %p\n", (void*)&var); //0x.... 

    // // menampilkan nilai dan alamat memori yang disimpan dalam pointer ptr
    // printf("Nilai yang disimpan dalam pointer ptr: %p\n", ptr); 
    // printf("Nilai yang ditunjuk oleh pointer ptr: %d\n", *ptr);

    // // mengubah nilai dari var melalui pointer ptr
    // *ptr = 20;

    // // menampilkan nilai var setelah diubah melalui pointer
    // printf("Nilai dari var setelah diubah melalui pointer: %d\n", var);
    int arr[] = {1, 2, 3, 4,5};
    printf()

    return 0;
}